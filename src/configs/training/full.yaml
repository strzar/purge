# Full training configuration for production runs
num_epochs: 100
per_device_train_batch_size: 4
gradient_accumulation_steps: 8
num_generations: 8
logging_steps: 10
save_strategy: "steps"
save_steps: 1000
save_total_limit: 3

# Dataset options
dataset_size: null  # Use full dataset

